// To parse this JSON data, do
//
// final restriccion = restriccionFromJson(jsonString);

/* import 'dart:convert';

Restriccion restriccionFromJson(String str) =>
    Restriccion.fromJson(json.decode(str));

String restriccionToJson(Restriccion data) => json.encode(data.toJson());

class Restriccion {
  Restriccion({
    required this.prefijo,
  });

  String prefijo;

  factory Restriccion.fromJson(Map<String, dynamic> json) => Restriccion(
        prefijo: json["Prefijo"],
      );

  Map<String, dynamic> toJson() => {
        "Prefijo": prefijo,
      };
}
 */